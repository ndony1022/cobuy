/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['image.coupang.com', 'thumbnail.coupang.com', 'via.placeholder.com'],
  },
}

module.exports = nextConfig

